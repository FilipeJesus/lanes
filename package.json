{
  "name": "claude-orchestra",
  "displayName": "Claude Orchestra",
  "publisher": "claude-orchestra",
  "description": "Manage Claude Code sessions via Git Worktrees",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:claudeSessionsView",
    "onCommand:claudeWorktrees.createSession"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "claude-orchestra-sidebar",
          "title": "Claude Orchestra",
          "icon": "$(robot)"
        }
      ]
    },
    "views": {
      "claude-orchestra-sidebar": [
        {
          "id": "claudeSessionsView",
          "name": "Active Sessions"
        }
      ]
    },
    "commands": [
      {
        "command": "claudeWorktrees.createSession",
        "title": "New Session",
        "icon": "$(plus)"
      },
      {
        "command": "claudeWorktrees.deleteSession",
        "title": "Delete Session",
        "icon": "$(trash)"
      },
      {
        "command": "claudeWorktrees.openSession",
        "title": "Open Session"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "claudeWorktrees.createSession",
          "when": "view == claudeSessionsView",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "claudeWorktrees.deleteSession",
          "when": "view == claudeSessionsView",
          "group": "inline"
        }
      ]
    },
    "keybindings": [
      {
        "command": "claudeWorktrees.createSession",
        "key": "ctrl+shift+c",
        "mac": "cmd+shift+c",
        "when": "editorTextFocus"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.75.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.39.1",
    "husky": "^9.1.7",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.48.1"
  }
}